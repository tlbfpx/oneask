server:
  port: 8081

spring:
  application:
    name: customer-service-agent
  ai:
    dashscope:
      api-key: ${AI_DASHSCOPE_API_KEY:}
      chat:
        options:
          model: deepseek-v3.2
    alibaba:
      a2a:
        nacos:
          server-addr: ${NACOS_ADDR:127.0.0.1:8848}
          username: ${NACOS_USERNAME:nacos}
          password: ${NACOS_PASSWORD:}
          discovery:
            enabled: false
          registry:
            enabled: true
        server:
          version: 1.0.0
          card:
            name: customer_service_agent
            description: 专业客服Agent，处理客户咨询、售后服务、地址查询、经纬度查询等问题
            provider:
              name: OneAsk
              organization: OneAsk
              url: https://github.com/oneask

app:
  baidu:
    map-ak: ${BAIDU_MAP_AK:}
  python:
    path: ${PYTHON_PATH:python3}

logging:
  level:
    com.oneask: DEBUG
    com.alibaba.cloud.ai: DEBUG
